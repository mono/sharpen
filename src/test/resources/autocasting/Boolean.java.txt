package autocasting;

class Autocasting {

    private boolean _b;
    private Autocasting(boolean b){
        _b = b;
    }

	public boolean booleanval(int x){
	    Boolean val = true && _b;
        Boolean res = resolve(val, val || (x == 2));
        if(res != null){
            return res || (x == 3);
        }

        return false;
    }

    private boolean resolve(boolean arg1, Boolean arg2){
        Boolean result = arg1 && arg2;
        if(result)
            return true;

        Autocasting obj = new Autocasting(result);
        result = obj.booleanval(10);
        return result;
    }
}